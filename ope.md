ワークアウトの targt の主力が wat が単位になっているのですが、これを pwr (power wait ratio) 体重60kg で pwr 1 なら 60w
をターゲットとするようにアプリに表示するようなイメージです。
変更
- バックエンドがもつ workout block のパラメメータを pwr (1,1.5,2) のようにする
- フロントエンドはバックエンドから貰った pwr をもとに登録されている体重をかけて XXX[w] の表示を組み立てる
  - 現状は体重登録がないので登録の画面を追加する。これは新たにメニュー画面を作成し、そこに体重・BLE検索/登録の導線を置く。体重登録は導線タップでダイアログを表示して、そこで体重を登録できるくらいのシンプルさでいい。


まずはどのような変更をするのか計画を教えてください。  