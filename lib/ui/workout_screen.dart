import 'package:flutter/widgets.dart';

class PowerMeter extends StatelessWidget {
  const PowerMeter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Power Meter");
  }
}
